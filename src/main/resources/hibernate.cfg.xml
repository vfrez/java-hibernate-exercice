<?xml version = "1.0" encoding = "utf-8"?>
<!DOCTYPE hibernate-configuration SYSTEM
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>

        <!-- Start database connection properties -->
        <property name = "hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
        </property>

        <property name = "hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <property name = "hibernate.connection.url">
            jdbc:mysql://localhost:3306/dados
        </property>

        <property name = "hibernate.connection.username">
            root
        </property>

        <property name="hibernate.connection.password">
            root
        </property>
        <!-- End database connection properties -->

        <property name="hibernate.show_sql">
            true
        </property>

        <property name="hibernate.format_sql">
            false
        </property>

        <property name="hibernate.use_sql_comments">
            false
        </property>

        <property name="hibernate.jdbc.wrap_result_sets">
            false
        </property>

        <property name="hibernate.hibernate.cache.use_query_cache">
            true
        </property>

        <property name="hibernate.hbm2ddl.auto">
            update
        </property>

        <!--Enable use of deprecated hibernate file mapping. DadosTable.hbm.xml-->
        <property name="hibernate.transform_hbm_xml.enabled">
            true
        </property>

        <!-- List of XML mapping files -->
        <mapping resource = "DadosTable.hbm.xml"/>

    </session-factory>
</hibernate-configuration>